{
  "name": "marquis-generated/{{name}}",
  "authors": [
    {
      "name": "{{name}}"
    }
  ],
  "config": {
    "vendor-dir": "{{vendorDir}}",
    "bin-dir": "{{binDir}}"
  },
  "require": {
    "twig/twig": "3.3"
  }
}
