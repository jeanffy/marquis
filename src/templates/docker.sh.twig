[[ $(docker ps -a -f name=^{{containerName}}$ -q) ]] && (docker rm -f {{containerName}})

docker run \
  -d \
  --rm \
  -p 8900:80 \
  --name {{containerName}} \
  -v "{{htmlFolder}}":/var/www/html \
  php:8-apache \
  /bin/bash -c 'a2enmod rewrite; apache2-foreground'
